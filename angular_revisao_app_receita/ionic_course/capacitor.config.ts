import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.receitas_first.app',
  appName: 'ionic_course',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
